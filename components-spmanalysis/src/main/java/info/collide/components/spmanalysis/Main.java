package info.collide.components.spmanalysis;


/**
 * Hello world!
 *
 */
public class Main 
{
    public static void main( String[] args )
    {
//    	  String serverlocation = SISOBProperties.getServerName(); 
         // int port = Integer.parseInt(serverdata[1]);
//           int port = SISOBProperties.getServerPort();
////    	String serverlocation = "glendronach.inf.uni-due.de";
////    	int port = 2525;
//        executeFileLoader(serverlocation, port);
    }
    
    public static void executeFileLoader(String serverlocation, int port) {
//        AgentManager flm = new SPMAnalysisManager(new Tuple(String.class, Integer.class, Integer.class, String.class, "Sequential Pattern Mining", String.class, String.class), "SPM Analysis Manager", serverlocation, port);
//        flm.initialize();
//        Thread runtime = new Thread(flm);
//        runtime.start();
    }  
}
